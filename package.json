{
  "name": "babel-plugin-drop-hof",
  "version": "1.0.0",
  "description": "Transforms higher-order function calls such as `map`, `filter`, and `reduce` to loops.",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf build && babel -d build/ src/",
    "build:dev": "babel -s -d build/ src/",
    "prepublish": "npm run build && npm test",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yehzhang/babel-plugin-drop-hof.git"
  },
  "author": "Simon Zhang",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/yehzhang/babel-plugin-drop-hof/issues"
  },
  "homepage": "https://github.com/yehzhang/babel-plugin-drop-hof#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^21.2.0",
    "babel-preset-env": "^1.6.0",
    "jest": "^21.2.0",
    "regenerator-runtime": "^0.11.0"
  },
  "dependencies": {
    "babel-core": "^6.26.0",
    "babel-template": "^6.26.0",
    "babel-types": "^6.26.0"
  }
}
